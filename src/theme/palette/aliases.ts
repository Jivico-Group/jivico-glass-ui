export const buildAliasesPalette = (isDark: boolean) => ({
  "glass-surface": {
    main: isDark ? "#FFFFFF" : "#000000",
    light: isDark ? "#FFFFFF" : "#000000",
    dark: isDark ? "#FFFFFF" : "#000000",
    contrastText: isDark ? "#000000" : "#FFFFFF",
  },

  glassSurface: {
    main: isDark ? "#FFFFFF" : "#000000",
    light: isDark ? "#FFFFFF" : "#000000",
    dark: isDark ? "#FFFFFF" : "#000000",
    contrastText: isDark ? "#000000" : "#FFFFFF",
  },
});
